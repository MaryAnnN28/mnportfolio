import React from 'react'
import { Card, CardMedia, CardAction, CardContent, Chip, Typography } from '@material-ui/core'

const ProjectCard = () => {
	return <div></div>
}

export default ProjectCard
